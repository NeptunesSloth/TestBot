[project]
name = "polymarket-money-printer-lite"
version = "0.3.0"
description = "Simple selective Polymarket-only crypto arbitrage bot with paper/live modes and SQLite dashboard."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
